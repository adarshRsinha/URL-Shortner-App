<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
</head>
<style>
    body{
        background-color: rgb(185, 187, 189);
        color: rgb(17, 16, 16);
        display: flex;
        justify-content: center;
    }

    .box{
        border-radius: 49.09px;
        background-color: var(--Form-Bg, #f6fbf9);
        display: flex;
        margin-top: 72px;
        width: 500px;
        max-width: 100%;
        flex-direction: column;
        align-items: center;
        padding: 20px 12px;
    }

    button{
        border-radius: 23.011px;
        border: 0px;
        background-color: #84c7ae;
        margin-top: 33px;
        width: 300px;
        max-width: 100%;
        align-items: center;
        color: #fff;
        display: flex;
        justify-content: center;
        padding: 14px 40px;
        font: 600 25px/130% Karla, -apple-system, Roboto, Helvetica,
        sans-serif;
        cursor: pointer;
    }

    form{
        display: ruby;
        justify-content: center;
        padding: 34px 36px;
    }

    input{
        padding: 20px;
        margin: 15px;
        width: 350px;
    }
</style>
<body>
    <div class="box">
        <h1><b>Login</b></h1>
        <form method="post" action="/user/login">
            <input type="text" required name="email" placeholder="Email">
            <input type="text" required name="password" placeholder="Password">
    
            <button type="submit">Login</button>
        </form>
        <span>Don't have an Account</span><a href="/signup">Sign Up</a>
    </div>
</body>
</html>